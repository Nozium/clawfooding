#!/usr/bin/env bash
set -euo pipefail

if command -v clawfooding >/dev/null 2>&1; then
  exec clawfooding "$@"
fi

if [ -x "$PWD/result/bin/clawfooding" ]; then
  exec "$PWD/result/bin/clawfooding" "$@"
fi

echo "clawfooding is not available. Run: nix build" >&2
exit 1
